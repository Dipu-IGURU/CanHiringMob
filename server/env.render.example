# Render Environment Variables Configuration
# Copy these to your Render service environment variables

# Database Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/canhiring?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random
JWT_EXPIRE=7d

# Server Configuration
NODE_ENV=production
PORT=10000

# Optional: If you want to use external MongoDB Atlas
# MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/canhiring?retryWrites=true&w=majority

# Optional: If you want to use Render's built-in database
# MONGODB_URI will be automatically set by Render if you use their database service

# CORS Configuration (optional - server handles this automatically)
# ALLOWED_ORIGINS=https://your-frontend-domain.com,https://your-mobile-app.com
